# Copy this file to ".env" and fill in. ".env" is gitignored (per-machine values).
API_SCHEME=http
# API_HOST=            # leave unset for sim/emulator (auto); set your LAN IP for a physical device
API_PORT=5001
STRIPE_PUBLISHABLE_KEY=
